name: Atualizar séries do BCB (dólar e Selic)

on:
  schedule:
    - cron: "0 3 * * *" # 03:00 UTC diário (00:00 BRT)
  workflow_dispatch: {} # permite disparo manual pela aba Actions

jobs:
  fetch:
    runs-on: ubuntu-latest
    environment: production
    permissions:
      contents: read
    env:
      DATABASE_URL: ${{ secrets.NEON_DATABASE_URL }}
    steps:
      - name: Checkout do repositório
        uses: actions/checkout@v5

      - name: Configurar Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Validar DATABASE_URL
        run: |
          if [ -z "$DATABASE_URL" ]; then
            echo "ERRO: configure NEON_DATABASE_URL como secret do Environment production."
            exit 1
          fi

      - name: Atualizar pip e instalar dependências
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Criar tabela, se necessário
        run: python -c "from config.database import create_tables; create_tables()"

      - name: Rodar ingestão
        run: python -c "from scripts.etl import load_data; load_data()"
